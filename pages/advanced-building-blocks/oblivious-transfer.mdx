# 混淆传输（Oblivious Transfer，简称 OT）

混淆传输（Oblivious Transfer，简称 OT）是一种加密原语，用于发送者向接收者传输可能包含许多信息的其中一部分。发送者不知道哪些信息被传输了。

混淆传输对于许多安全多方计算的构建至关重要。在其最基本的形式中，发送者有两个秘密消息作为输入 m_0，m_1；接收者有一个选择位 c 作为输入。在 1-out-of-2 OT 协议结束时，接收者只应学习消息 m_c，而发送者不应学习接收者输入 c 的值。
