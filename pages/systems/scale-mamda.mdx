# SCALE-MAMBA

SCALE-MAMBA 是一个基于大有限域和二元域上的秘密共享的真正多方计算框架。它在主动安全性中是安全的，并且具有放弃模型。该系统包含多个钩子，以使其可适用于接近真实生活的情况。

MAMBA 是前端语言。它将类似 Python 的语言编译成字节码表示形式。字节码在文档中完全规定。

SCALE 是运行时，它执行基于字节码的安全协议。

该系统基于 SPDZ 实现大有限域计算，在离线阶段使用 TopGear。对于二元域，该系统使用 BMR 协议的一种变体，称为 HSS，使用 Tiny-OT 来生成电路的共享混淆。可以在大素数有限域共享和二元共享之间交换数据。
